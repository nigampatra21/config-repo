global:
  message: welcome to the order service dev Environment!
seerver:
  port: 8084
message:
  welcome: Hello from Order Dev!
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/order_dev
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
