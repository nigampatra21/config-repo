global:
  message: welcome to the order service prod Environment!
seerver:
  port: 8085
message:
  welcome: Hello from order Prod!
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/order_prod
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
