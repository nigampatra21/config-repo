global:
  message: welcome to thecustomer service prod Environment!
seerver:
  port: 8083
message:
  welcome: Hello from prod!
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cust_prod
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
